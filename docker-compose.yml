version: '2'
services:
  pizzaDay:
    build: ./PizzaDay
    image: PizzaDay
    links:
    - MongoDB
    ports:
     - "80:9000"
    volumes:
     - /opt/mongodb/db:/data/db
    depends_on:
     - MongoDB
  mongoDB:
    build: ./MongoDB
    image: MongoDB