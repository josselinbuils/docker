version: '2'
services:
  PizzaDay:
    build: ./PizzaDay
    image: PizzaDayImg
    links:
    - mongoDB
    ports:
     - "80:9000"
    volumes:
     - /opt/mongodb/db:/data/db
    depends_on:
     - mongoDB
  MongoDB:
    build: ./MongoDB
    image: MongoDBImg